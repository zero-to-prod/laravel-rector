# Instructions

Run `composer check` at the end of a sessions to verify correctness.

## Commands

```shell
composer check # Does not mutate
composer fix # Mutates artifacts
composer mcp list # tool names and descriptions
composer mcp call <tool> '{"k":"v"}' # with arguments
```
