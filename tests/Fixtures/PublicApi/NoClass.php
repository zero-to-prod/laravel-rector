<?php

declare(strict_types=1);

// Intentionally declares nothing, so the renderer has a PHP file that maps to
// no class. Keep it free of declarations: the autoloader may include it more
// than once while failing to find the class this path implies.
